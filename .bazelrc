# Import bazelrc presets
import %workspace%/tools/preset.bazelrc

########################
# Project-specific flags
# This is also a place to override settings from the presets

# Starting with Bazel 8, Windows requires this flag in order
# for symlinks to work properly (namely, so that sh_test with
# py_binary as a data dependency gets symlinks that are executable)
startup --windows_enable_symlinks
startup --noautodetect_server_javabase

common --announce_rc --show_timestamps

common --verbose_failures

# Enable Bzlmod for every Bazel command
common --enable_bzlmod --noenable_workspace

# Tell Bazel to not detect the CC toolchain automatically from the host machine to improve hermeticity.
# This requires you to declare and register a CC toolchain explicitly.
common --repo_env=BAZEL_DO_NOT_DETECT_CPP_TOOLCHAIN=1

# Enable Debug Fission
common:linux --fission=yes
common --compilation_mode=dbg

# Turn on all the strict error checking for CC
common --copt=-g3  # Add more debug stuff
# common --copt=-pedantic-errors
common --cxxopt=-std=c++23
# common --cxxopt=-Weffc++
# common --cxxopt=-Wextra
# common --cxxopt=-Wconversion
# common --cxxopt=-Wsign-conversion
# common --cxxopt=-Werror

# C Options
common --conlyopt=-std=c23  # Needed for c programs and not angering c++ ones

common:windows --enable_runfiles # https://github.com/bazelbuild/bazel/issues/8843

# Visual Studio 2022
## NOTE: We need to have WSL running in the background for some of the compilation process
# common:windows --define compiler=vs2022
common:clang_windows --cxxopt=/std:c++20  # Qt requires a C++17 compiler, and a suitable value for __cplusplus.
common:clang_windows --cxxopt=/Zc:__cplusplus #  On MSVC, you must pass the /Zc:__cplusplus option to the compiler.
common:clang_windows --cxxopt=/permissive-  # QtCore/qglobal.h(120): error C2338
common:clang_windows --cxxopt=/Z7
common:clang_windows --copt=-DWIN32_LEAN_AND_MEAN
common:clang_windows --copt=-DNOGDI
common:clang_windows --host_copt=-DWIN32_LEAN_AND_MEAN
common:clang_windows --host_copt=-DNOGDI
common:clang_windows --host_cxxopt=/std:c++23


###### This block of code works when we have rules_cc local_config_cc enabled
# As of bazel 7, toolchain resolution is enabled by default, so we need to define a platform for
# the clang-cl build.
build:clang_windows --extra_toolchains=@local_config_cc//:cc-toolchain-x64_windows-clang-cl
build:clang_windows --extra_execution_platforms=//:x64_windows-clang-cl
###### End block of rules_cc local_config_cc enablement

# We need to turn off pedantic copts for LLVM by Source on Windows (or on MinGW)
# external/llvm+/runtimes/empty.c:1:1: error: ISO C requires a translation unit to contain at least one declaration [-Werror,-Wempty-translation-unit]
# ^For linux for sure...
common:linux --per_file_copt='external/llvm.*@-Wno-error,-Wno-pedantic'
common:linux --host_per_file_copt='external/llvm.*l@-Wno-error,-Wno-pedantic'


# XXX: this doesn't work as intended, but we'll keep it here - for both Windows AND Linux
common --per_file_copt='external/rules_qt.*l@-Wno-error,-Wno-pedantic'
common --host_per_file_copt='external/rules_qt.*l@-Wno-error,-Wno-pedantic'
